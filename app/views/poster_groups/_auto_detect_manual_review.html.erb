<div class="p-3 rounded-lg bg-blue-500/10 border border-blue-500/30">
  <div class="flex items-start gap-2">
    <%= icon :clock, size: 16, class_name: "text-blue-600 flex-shrink-0 mt-0.5" %>
    <div class="flex-1 min-w-0">
      <p class="text-sm font-bold text-blue-600">Submitted for Manual Review</p>
      <p class="text-xs campaign-text-secondary mt-1">
        <%= reason %>. Your proof has been attached to poster #<%= poster.id %> and will be manually reviewed.
      </p>
    </div>
  </div>
</div>

<script>
  // Refresh the page to show updated poster status
  setTimeout(() => window.location.reload(), 1500);
</script>
