<% content_for :title, "Edit #{@shop_item.name}" %>

<div class="mb-8">
  <h1 class="text-3xl font-bold tracking-tight text-foreground">Edit <%= @shop_item.name %></h1>
  <p class="text-sm text-muted-foreground mt-2">Update product information.</p>
</div>

<div class="rounded-lg border border-border bg-card p-6 shadow-sm">
  <%= render "form", shop_item: @shop_item %>
</div>
