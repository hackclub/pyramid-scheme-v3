# examples:
#   periodic_cleanup:
#     class: CleanSoftDeletedRecordsJob
#     queue: background
#     args: [ 1000, { batch_size: 500 } ]
#     schedule: every hour
#   periodic_cleanup_with_command:
#     command: "SoftDeletedRecord.due.delete_all"
#     priority: 2
#     schedule: at 5am every day

production:
  # NOTE: This file is no longer used - we now use a simple loop in bin/jobs
  # that enqueues AirtableSyncJob every minute using ActiveJob async adapter
  # (in-memory, no external dependencies)

development:
  # NOTE: This file is no longer used - we now use a simple loop in bin/jobs
  # that enqueues AirtableSyncJob every minute using ActiveJob async adapter
  # (in-memory, no external dependencies)
